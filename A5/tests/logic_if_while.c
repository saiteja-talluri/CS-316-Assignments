int a,b;
float c;

void main(){
	int a,d;
	float e;
	a = 4;
	b = 2;
	d = 3;
	c = 4.0;
	e = 3.0; 

	if(!(a < b) && (a < d) || (c < e))
		if(!(a < b) && (a < d) || (c < e))
			a = a + a*b;
		else
			while(c < e + c)
				c = c + e;
}